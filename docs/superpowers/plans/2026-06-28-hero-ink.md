# 首页首屏水墨卷轴实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把首页首屏升级为全屏水墨卷轴，强化品牌首屏印象。

**Architecture:** 用 Next.js Image 做全屏背景，CSS keyframes 做呼吸与文字浮现动画，滚动监听做视差淡出；所有改动集中在 `Hero.tsx`、`globals.css`、`page.tsx` 与 `Features.tsx`。

**Tech Stack:** Next.js 16 + React + TypeScript + Tailwind CSS v4

---

### Task 1: 生成国风山水 Hero 背景图

**Files:**
- Create: `public/images/hero-ink.jpg`

- [ ] **Step 1: 生成背景图**

使用 AI 图像生成工具，提示词：

```
[PURPOSE: Hero background for Chinese classical literature revitalization website]: A wide panoramic Chinese ink wash landscape painting, misty mountains and ancient pine trees, soft gray and sepia tones, rice paper texture, minimalist composition with large empty sky area on the left for text overlay, serene atmosphere, traditional shanshui style, high resolution.
```

尺寸：`landscape_16_9`（1280×720）或 `1920x1080`。
保存路径：`/workspace/ancient-scroll/public/images/hero-ink.jpg`。

- [ ] **Step 2: 确认图片存在**

Run:
```bash
ls -la /workspace/ancient-scroll/public/images/hero-ink.jpg
```
Expected: file exists.

---

### Task 2: 添加 Hero 动画 CSS

**Files:**
- Modify: `app/globals.css`

- [ ] **Step 1: 在 `app/globals.css` 末尾追加 Hero 相关 keyframes**

```css
/* Hero background breathe */
@keyframes hero-breathe {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.08); }
}

.animate-hero-breathe {
  animation: hero-breathe 20s ease-in-out infinite alternate;
}

/* Hero reveal */
@keyframes hero-reveal {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-hero-reveal {
  opacity: 0;
  animation: hero-reveal 0.7s ease-out forwards;
}

/* Reduced motion: hero animations */
@media (prefers-reduced-motion: reduce) {
  .animate-hero-breathe,
  .animate-hero-reveal {
    animation: none !important;
    opacity: 1 !important;
    transform: none !important;
  }
}
```

- [ ] **Step 2: Commit CSS**

```bash
git add app/globals.css
git commit -m "feat: add hero ink scroll animations"
```

---

### Task 3: 重写 Hero 组件

**Files:**
- Modify: `components/Hero.tsx`

- [ ] **Step 1: 用以下内容替换 `components/Hero.tsx` 全部内容**

```tsx
"use client";

import Image from "next/image";
import Link from "next/link";
import { useEffect, useState } from "react";

const title = "古籍焕新";

export default function Hero() {
  const [scrollY, setScrollY] = useState(0);

  useEffect(() => {
    const handleScroll = () => setScrollY(window.scrollY);
    window.addEventListener("scroll", handleScroll, { passive: true });
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  const progress = Math.min(
    scrollY / (typeof window !== "undefined" ? window.innerHeight : 1),
    1
  );
  const bgOpacity = 1 - progress * 0.7;
  const bgY = -progress * 15;

  return (
    <section className="relative h-svh w-full overflow-hidden">
      {/* Background image with parallax */}
      <div
        className="absolute inset-0 -z-20 will-change-transform"
        style={{
          opacity: bgOpacity,
          transform: `translateY(${bgY}%)`,
        }}
      >
        <Image
          src="/images/hero-ink.jpg"
          alt="水墨山水"
          fill
          priority
          className="animate-hero-breathe object-cover"
          sizes="100vw"
        />
      </div>

      {/* Gradient overlay for text readability */}
      <div className="absolute inset-0 -z-10 bg-gradient-to-r from-xuan/80 via-xuan/50 to-xuan/20" />
      <div className="absolute inset-0 -z-10 bg-gradient-to-t from-xuan via-transparent to-transparent" />

      {/* Foreground content */}
      <div className="relative z-10 flex h-full flex-col justify-center px-6 md:items-start md:px-12 lg:px-20">
        <h1
          className="mb-6 text-center font-calligraphy tracking-wider md:text-left"
          style={{ fontSize: "clamp(3rem, 12vw, 7rem)" }}
        >
          {title.split("").map((char, i) => (
            <span
              key={i}
              className="inline-block animate-hero-reveal text-ink"
              style={{ animationDelay: `${0.2 + i * 0.08}s` }}
            >
              {char}
            </span>
          ))}
        </h1>

        <p
          className="mb-8 text-center font-handwrite text-2xl text-light-ink md:text-left md:text-3xl animate-hero-reveal"
          style={{ animationDelay: "0.7s" }}
        >
          让千年文字“活”起来
        </p>

        <Link
          href="#features"
          className="mx-auto inline-flex items-center gap-2 rounded-full bg-cinnabar px-6 py-3 font-serif text-sm text-white shadow-md transition-transform hover:translate-x-1 md:mx-0 animate-hero-reveal"
          style={{ animationDelay: "1s" }}
        >
          开启旅程
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="2"
            strokeLinecap="round"
            strokeLinejoin="round"
            className="h-4 w-4"
          >
            <path d="M5 12h14" />
            <path d="m12 5 7 7-7 7" />
          </svg>
        </Link>
      </div>

      {/* Seal badge */}
      <div className="absolute bottom-8 right-8 z-10 flex h-16 w-16 rotate-[-3deg] items-center justify-center rounded-sm bg-seal-bg shadow-sm md:bottom-12 md:right-12 md:h-20 md:w-20">
        <span className="text-center font-calligraphy text-xs leading-tight text-seal-red md:text-sm">
          古籍
          <br />
          焕新
        </span>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit Hero rewrite**

```bash
git add components/Hero.tsx
git commit -m "feat: rewrite Hero as full-screen ink scroll"
```

---

### Task 4: 为 CTA 锚点添加 `id="features"`

**Files:**
- Modify: `components/Features.tsx`

- [ ] **Step 1: 在 `Features.tsx` 根 `<section>` 上添加 id**

Find:
```tsx
<section className="fade-in relative w-full py-16 md:py-24">
```

Replace with:
```tsx
<section id="features" className="fade-in relative w-full py-16 md:py-24">
```

- [ ] **Step 2: Commit**

```bash
git add components/Features.tsx
git commit -m "feat: add features anchor for hero CTA"
```

---

### Task 5: 构建与验证

**Files:**
- Verify: `.next/server/app/index.html`

- [ ] **Step 1: 安装依赖并构建**

```bash
npm install && npm run build
```

Expected: exit code 0, no TypeScript errors.

- [ ] **Step 2: 验证首页输出包含新 Hero 结构**

```bash
grep -o 'hero-ink.jpg' .next/server/app/index.html | head -1
```

Expected: `hero-ink.jpg`

- [ ] **Step 3: 验证没有旧 falling chars 残留**

```bash
grep -c 'animate-fall-char' .next/server/app/index.html || true
```

Expected: `0`

---

### Task 6: 推送代码

- [ ] **Step 1: Push to GitHub**

```bash
git push origin main
```

Expected: pushes successfully.

---

## Self-Review

- **Spec coverage:**
  - 全屏水墨背景 → Task 1 + Task 3
  - 标题逐字浮现 → Task 3 CSS + map
  - 副标题/CTA 依次出现 → Task 3
  - 滚动视差淡出 → Task 3 `useEffect` + transform/opacity
  - 右下角印章 → Task 3
  - 移动端适配 → Task 3 responsive classes + clamp
  - reduced motion → Task 2 media query
  - CTA 锚点 → Task 4

- **Placeholder scan:** 所有步骤均含具体代码与命令，无 TBD/TODO。
- **Type consistency:** `useState<number>` 通过泛型推断，scroll 计算使用 `window` 守卫。
